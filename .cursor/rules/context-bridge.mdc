---
description: 上下文桥接机制，防止 API 账号切换后上下文丢失
alwaysApply: true
---

# 上下文桥接（防止账号切换后上下文丢失）

## 问题背景
AI API 账号达到额度上限后会自动切换到新账号，新会话丢失之前的分析上下文，导致从头重复探索、原地踏步。本规则通过文件检查点机制解决此问题。

## 会话恢复（每次新响应的第一步）
1. 读取项目根目录 `agent-context.md`
2. 如果文件存在且"下一步"字段非空：
   - 继承其中记录的所有已分析上下文
   - 直接执行"下一步"描述的具体动作
   - 不要重新探索"已分析的关键文件"列表中的文件
3. 如果文件不存在或"下一步"为空，正常开始工作

## 工作过程中（更新检查点）
完成以下任一动作后，立即更新 `agent-context.md`：
- 读完一个新文件并得出关键结论 -> 追加到"已分析的关键文件"
- 写入或修改了代码 -> 追加到"已完成的改动"
- 验证通过/失败 -> 更新"验证状态"
- 发现新的问题或约束 -> 追加到"关键发现"
- 确定了下一步动作 -> 更新"下一步"

## 反重复约束（防止原地踏步）
- 不要重复读取"已分析的关键文件"列表中的文件，除非有明确证据表明文件内容已变化
- 不要重复宣布"我已掌握全部上下文"等，直接执行下一步
- 如果当前动作和 `agent-context.md` 记录的"下一步"完全相同，说明你在原地踏步，立即切换到新的下一步动作
- 探索阶段最多不超过 5 个文件，之后必须开始写代码或给出明确结论

## 检查点文件格式（agent-context.md）
```
## 当前任务
[一句话描述任务目标]

## 已分析的关键文件
- [文件路径]: [关键结论]

## 已完成的改动
- [文件路径]: [改了什么]

## 验证状态
- [验证项]: [通过/失败/待验证]

## 关键发现
- [发现]

## 下一步
[具体操作描述]
```

## 更新规则
- 每次更新只覆盖内容，不追加历史版本
- 这不是进度日志，是工作状态快照
- 任务完成后清空内容，写"任务已完成"
